set :output, "#{path}/log/cron.log"

every '<%= @config[:time][:every] %>'<%= ", :at => '#{@config[:time][:at]}'" if @config[:time][:at] %> do
  command "backup perform --trigger <%= @config[:name] %>"
end
